3.	Write a program to read a score between 1 and 100 for a quiz from the user, assigns a letter grade (according to the following table) using if statement, and print the grade.

score = int(input("Enter score between 1 and 100: "))
if score < 40:
    grade = 'F'
elif score < 50:
    grade = 'E'
elif score < 60:
    grade = 'D'
elif score < 70:
    grade = 'C'
elif score < 80:
    grade = 'B'
else:
    grade = 'A'

print("Your grade is %s" %(grade))
